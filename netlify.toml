[dev]
  port = 8888
  publish = "www"

[build]
  command = "npm run build"
  publish = "www"

# Regla imprescindible para SPAs (Angular): redirigir todo el tráfico que no sea un archivo a index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
